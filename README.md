# Interactive Line Chart - A/B Test Statistics

Интерактивное приложение для визуализации статистики A/B тестов с помощью линейного графика.

## 🎯 Цель проекта

Построить интерактивный **Line Chart** для визуализации статистики A/B тестов с отображением conversion rate для различных вариаций.

## 📚 Используемые технологии

### Основной стек
- **React 18** + **TypeScript** - основной фреймворк
- **Vite** - инструмент сборки (быстрая разработка, оптимизированная сборка)
- **Recharts** - библиотека для графиков (простая интеграция с React, хорошая документация)
- **CSS Modules** - стилизация компонентов
- **gh-pages** - деплой на GitHub Pages

### Дополнительные библиотеки
- **date-fns** - работа с датами (агрегация по неделям)
- **html2canvas** - экспорт графика в PNG (бонусная функция)

## ✨ Реализованные функции

### Обязательные требования
- ✅ Отображение conversion rate в процентах для всех вариаций
- ✅ Hover с вертикальной линией и попапом с данными
- ✅ Минимум одна вариация всегда выбрана
- ✅ Автоматическая адаптация осей X и Y на основе видимых данных
- ✅ Селектор вариаций (мультиселект с чекбоксами)
- ✅ Переключатель Day/Week с агрегацией данных
- ✅ Адаптивный дизайн для экранов от 671px до 1300px

### Бонусные функции
- ✅ Zoom In/Out и Reset zoom
- ✅ Выбор стиля линии (Line, Smooth, Area)
- ✅ Переключатель темы (Light/Dark)
- ✅ Экспорт графика в PNG

## 🚀 Локальная установка и запуск

### Требования
- Node.js версии 18 или выше
- npm или yarn

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd code
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## 📦 Деплой на GitHub Pages

### Настройка

1. Убедитесь, что в `vite.config.ts` указан правильный `base` path:
```typescript
base: '/code/',
```

2. Выполните деплой:
```bash
npm run deploy
```

Это создаст ветку `gh-pages` с собранным приложением и задеплоит его на GitHub Pages.

### Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. Выберите источник: `gh-pages` branch
3. Сохраните изменения

Приложение будет доступно по адресу: `https://<username>.github.io/code/`

## 📁 Структура проекта

```
src/
├── components/
│   ├── Chart/              # Компонент графика
│   ├── Controls/           # Компоненты управления (селекторы, кнопки)
│   ├── Tooltip/            # Кастомный tooltip
│   └── Layout/             # Основной layout приложения
├── hooks/
│   ├── useChartData.ts     # Хук для обработки данных графика
│   └── useTheme.ts         # Хук для управления темой
├── utils/
│   ├── dataProcessing.ts   # Обработка данных, расчет conversionRate
│   ├── dateUtils.ts        # Утилиты для работы с датами
│   └── chartUtils.ts       # Утилиты для графика (экспорт)
├── types/
│   └── index.ts            # TypeScript типы
├── data.json               # Исходные данные
├── main.tsx                # Точка входа
└── index.css               # Глобальные стили
```

## 🎨 Особенности реализации

### Обработка данных
- Данные загружаются из `data.json`
- Conversion rate рассчитывается по формуле: `(conversions / visits) * 100`
- Поддержка агрегации по дням и неделям
- Автоматическая фильтрация по выбранным вариациям

### График
- Используется библиотека Recharts для отрисовки
- Поддержка трех стилей линий: Line (линейный), Smooth (сглаженный), Area (с заливкой)
- Адаптивные оси, которые автоматически подстраиваются под видимые данные
- Вертикальная линия при hover для лучшей визуализации

### Интерактивность
- Кастомный tooltip с отображением всех вариаций
- Иконка трофея для лучшей вариации
- Zoom функциональность для детального просмотра
- Экспорт графика в PNG формат

### Темы
- Темная тема по умолчанию (согласно дизайну)
- Светлая тема (бонус)
- Сохранение выбранной темы в localStorage

## 📝 Лицензия

Этот проект создан в рамках тестового задания.
